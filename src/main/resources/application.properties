# Server Configuration
server.port=${SERVER_PORT:8080}
spring.application.name=flexreact-backend

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://db.unbyenhdrldevruaomfp.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:UpZcOlAsWVvMOTGK}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:fWajnXHQR5hv1Jh8sAgtz5SutkEIcQVqxnlBYfNX4GPO31kbty1bDwIeUGMX5KeiMydIRng0W8wjVHaqKw8tbQ==}
jwt.expiration=${JWT_EXPIRATION:3600}

# CORS Configuration
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Logging
logging.level.com.flexreact=DEBUG
logging.level.org.springframework.security=DEBUG
